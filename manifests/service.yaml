apiVersion: v1
kind: Service
metadata:
  labels:
    app: hands-on
  name: hands-on
spec:
  type: ClusterIP
  ports:
  - name: http-app
    port: 8080
    protocol: TCP
    targetPort: http
  selector:
    app: hands-on
