apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    policy.cilium.io/proxy-visibility: <Ingress/8080/TCP/HTTP>
  labels:
    app: hands-on
    color: blue
    version: v1
    prometheus-monitor-ignore: ""
  name: hands-on-blue
spec:
  replicas: 1
  revisionHistoryLimit: 1
  selector:
    matchLabels:
      app: hands-on
      color: blue
  strategy:
    rollingUpdate:
      maxSurge: 2
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: hands-on
        color: blue
        version: v1
        prometheus-monitor-ignore: ""
    spec:
      serviceAccountName: hands-on-blue
      containers:
      - image: argoproj/rollouts-demo:blue
        imagePullPolicy: IfNotPresent
        name: hands-on
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
        readinessProbe:
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
        resources:
          requests:
            memory: 84Mi
            cpu: 100m
          limits:
            memory: 84Mi
            cpu: 100m
        securityContext:
          runAsUser: 1000
          runAsGroup: 2000
          allowPrivilegeEscalation: false
      terminationGracePeriodSeconds: 15
---
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    policy.cilium.io/proxy-visibility: <Ingress/8080/TCP/HTTP>
  labels:
    app: hands-on
    color: yellow
    version: v1
    prometheus-monitor-ignore: ""
  name: hands-on-yellow
spec:
  replicas: 1
  revisionHistoryLimit: 1
  selector:
    matchLabels:
      app: hands-on
      color: yellow
  strategy:
    rollingUpdate:
      maxSurge: 2
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: hands-on
        color: yellow
        version: v1
        prometheus-monitor-ignore: ""
    spec:
      serviceAccountName: hands-on-yellow
      containers:
      - image: argoproj/rollouts-demo:yellow
        imagePullPolicy: IfNotPresent
        name: hands-on
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
        readinessProbe:
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
        resources:
          requests:
            memory: 84Mi
            cpu: 100m
          limits:
            memory: 84Mi
            cpu: 100m
        securityContext:
          runAsUser: 1000
          runAsGroup: 2000
          allowPrivilegeEscalation: false
      terminationGracePeriodSeconds: 15
